# Secrets — never commit. See CLAUDE.md: all secrets come from the environment at runtime.
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
id_rsa
id_ed25519
credentials.json
secrets.yaml
secrets.yml
.netrc

# Go build artifacts
/bin/
/dist/
*.exe
*.test
*.out
*.prof
coverage.out
go.work
go.work.sum

# Logs — request logging records user attributes, so these never get committed
/logs/
*.log

# Design mockups — working references for in-progress phases, not shipped artifacts
/docs/mockups/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Personal Claude Code overrides — never shared
.claude/settings.local.json
