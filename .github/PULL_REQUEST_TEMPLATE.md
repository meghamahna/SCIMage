## What this changes

Say what the change does and why.

## Checklist

- [ ] `make test` passes
- [ ] Code is formatted (`gofmt` / `goimports`)
- [ ] Tests added or updated for the change
- [ ] If it touches a security property (auth, tenant isolation, audit, secrets), I have called that out above
